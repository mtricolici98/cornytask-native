<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/unicorn_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/unicorn_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cornytaskv2">

        <service
            android:name=".features.timer.TimerService"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".features.widget.TodoWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.cornytask_v2.ACTION_DATA_UPDATED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/todo_widget_info" />
        </receiver>

        <receiver
            android:name=".features.actions.AddTaskReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="custom.actions.intent.ADD_CORNY_TASK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".features.login.LoginActivity"
            android:exported="false"
            android:label="Login"
            android:theme="@style/Theme.Cornytaskv2" />
        <activity
            android:name=".features.main.MainMenuActivity"
            android:exported="true"
            android:label="Main Menu"
            android:theme="@style/Theme.Cornytaskv2" >
        </activity>

        <activity
            android:name=".features.todo.AddTodoActivity"
            android:exported="false"
            android:resizeableActivity="true"
            android:launchMode="singleTop"
            android:label="Add Todo"
            android:theme="@style/Theme.Cornytaskv2" />

        <activity
            android:name=".features.todo.EditTodoActivity"
            android:exported="false"
            android:resizeableActivity="true"
            android:launchMode="singleTop"
            android:label="Edit todo"
            android:theme="@style/Theme.Cornytaskv2" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Cornytaskv2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="com.google.android.actions"
                android:resource="@xml/actions" />
        </activity>
    </application>

</manifest>